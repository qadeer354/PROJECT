export enum StorageKeys {
  token = 'token',
  user = 'user',
}

export enum RoutePath{
  login = "login",
  home = "home",
  chat = "chat",
}

export enum FirebaseCollections{
  users = "users",
  chats = "chats",
  messages = "messages",
}
